/**
 * Configuration for the 3D Camera
 * Controls the view angle and zoom level
 */
export const CAMERA_CONFIG = {
  position: [0, 0, 18] as [number, number, number], // Position standard
  fov: 30, // FOV standard
} as const;
